<phone:PhoneApplicationPage 
    x:Class="BeautyWeekly.Views.Main"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="800"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait"  Orientation="Portrait"
    shell:SystemTray.IsVisible="False"
    DataContext="{Binding MainViewModel, Source={StaticResource Locator}}">

    <!--LayoutRoot contains the root grid where all other page content is placed-->
    <Grid x:Name="LayoutRoot">
        <controls:Panorama>
            <controls:Panorama.TitleTemplate>
                <DataTemplate>
                </DataTemplate>
            </controls:Panorama.TitleTemplate>
            <!--Panorama item one-->
            <controls:PanoramaItem Header="item1">
                <Grid>
                    <ListBox toolkit:TiltEffect.IsTiltEnabled="True" ItemsSource="{Binding List1}" HorizontalAlignment="Left" VerticalAlignment="Top" BorderBrush="Transparent" Width="420">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid HorizontalAlignment="Left">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                <Button Command="{Binding comm}" Grid.Column="0" Margin="5" Template="{StaticResource EmptyButtonTemplate}" HorizontalAlignment="Left" VerticalAlignment="Top">
                                    <Grid>
                                        <Image Source="{Binding Path=[0].MainPicture}" Width="190" Height="190" />
                                        <Border Grid.Column="0" VerticalAlignment="Bottom" Height="50">
                                            <Border.Background>
                                                <SolidColorBrush Opacity="0.5" Color="Black"></SolidColorBrush>
                                            </Border.Background>
                                            <TextBlock TextWrapping="NoWrap" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Pink" Text="{Binding [0].Title}" FontSize="20" />
                                        </Border>
                                    </Grid>
                                </Button>
                                <Button Grid.Column="1" Margin="5" Template="{StaticResource EmptyButtonTemplate}" HorizontalAlignment="Left" VerticalAlignment="Top">
                                    <Grid>
                                        <Image Source="{Binding Path=[1].MainPicture}" Width="190" Height="190" />
                                        <Border Grid.Column="1" VerticalAlignment="Bottom" Height="50">
                                            <Border.Background>
                                                <SolidColorBrush Opacity="0.5" Color="Black"></SolidColorBrush>
                                            </Border.Background>
                                            <TextBlock TextWrapping="NoWrap" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Pink" Text="{Binding [1].Title}" FontSize="20" />
                                        </Border>
                                    </Grid>
                                </Button>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </controls:PanoramaItem>

            <!--Panorama item two-->
            <controls:PanoramaItem Header="item2">
                <Grid>
                    <ListBox toolkit:TiltEffect.IsTiltEnabled="True" ItemsSource="{Binding List2}" HorizontalAlignment="Left" VerticalAlignment="Top" BorderBrush="Transparent" Width="420">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid HorizontalAlignment="Left">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <Button Command="{Binding comm}" Grid.Column="0" Margin="5" Template="{StaticResource EmptyButtonTemplate}" HorizontalAlignment="Left" VerticalAlignment="Top">
                                        <Grid>
                                            <Image Source="{Binding Path=[0].MainPicture}" Width="190" Height="190" />
                                            <Border Grid.Column="0" VerticalAlignment="Bottom" Height="50">
                                                <Border.Background>
                                                    <SolidColorBrush Opacity="0.5" Color="Black"></SolidColorBrush>
                                                </Border.Background>
                                                <TextBlock TextWrapping="NoWrap" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Pink" Text="{Binding [0].Title}" FontSize="20" />
                                            </Border>
                                        </Grid>
                                    </Button>
                                    <Button Grid.Column="1" Margin="5" Template="{StaticResource EmptyButtonTemplate}" HorizontalAlignment="Left" VerticalAlignment="Top">
                                        <Grid>
                                            <Image Source="{Binding Path=[1].MainPicture}" Width="190" Height="190" />
                                            <Border Grid.Column="1" VerticalAlignment="Bottom" Height="50">
                                                <Border.Background>
                                                    <SolidColorBrush Opacity="0.5" Color="Black"></SolidColorBrush>
                                                </Border.Background>
                                                <TextBlock TextWrapping="NoWrap" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Pink" Text="{Binding [1].Title}" FontSize="20" />
                                            </Border>
                                        </Grid>
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </controls:PanoramaItem>
        </controls:Panorama>
    </Grid>

    <!--Panorama-based applications should not show an ApplicationBar-->

</phone:PhoneApplicationPage>